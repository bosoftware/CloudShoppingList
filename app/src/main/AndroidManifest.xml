<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bo.cloudshoppinglist" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name=".activity.CloudShoppingListApplication">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity"></activity>
        <activity android:name=".activity.RegisterActivity"></activity>
        <activity android:name=".activity.AddShoppingListActivity"></activity>
        <activity android:name=".activity.ShopListItemActivity"></activity>
        <activity android:name=".activity.AddShoppingListItemActivity"></activity>
        <activity android:name=".activity.PhotoListActivity"></activity>
        <activity android:name=".activity.DisplayImageActivity"></activity>
        <activity android:name=".activity.FriendShopListActivity"></activity>
        <activity android:name=".activity.AddFriendActivity"></activity>
        <activity android:name=".activity.ForgetPasswordActivity"></activity>
    </application>

</manifest>
